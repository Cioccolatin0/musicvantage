import type { Express } from "express";

export function registerStorageProxy(app: Express) {
  // Storage proxy routes can be added here
  // This is a placeholder for future storage functionality
}
