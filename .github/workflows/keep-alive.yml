name: Keep HF Space Alive

on:
  schedule:
    - cron: "0 */6 * * *"  # Every 6 hours
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping HF Space
        run: |
          curl -sf https://${{ secrets.HF_SPACE_URL }}/health || exit 1
